<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qi.myandroidstructure">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity android:name=".canaryLeak.LeakTestActivity"></activity>-->
        <!-- <activity android:name="com.example.qi.myandroidstructure.mvp.activities.ShowGirlActivity"></activity> -->
        <!-- <activity android:name=".IOC.IocActivity"></activity> -->
        <!-- <activity android:name=".seniorJava.SeniorJavaActivity" /> -->
        <!-- <activity android:name=".okhttp.OkhttpActivity"></activity> -->
        <!-- <activity android:name=".selfRxjava.RxJavaActivity"></activity> -->
<!--        <activity android:name=".selfEventBus.EventBusActivity" />-->
        <activity android:name=".canaryLeak.LeakTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>