<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qi.myandroidstructure">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".seniorJava.SeniorJavaActivity"></activity>
        <!-- <activity android:name=".okhttp.OkhttpActivity"></activity> -->
        <!-- <activity android:name=".selfRxjava.RxJavaActivity"></activity> -->
        <activity android:name=".selfEventBus.EventBusActivity" />
        <activity android:name=".okhttp.OkhttpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>